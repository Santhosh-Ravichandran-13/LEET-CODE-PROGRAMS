char* removeTrailingZeros(char* num) {
    int i;
    int len=strlen(num);
    for(i=len-1;i>0;i--){
       if(num[i]=='0'){
        num[i]='\0';
       }else{
        break;
       }
      
    }
    return num;
}
